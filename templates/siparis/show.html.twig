{% extends 'base.html.twig' %}

{% block title %}{{ sipari.title }}{% endblock %}

{% block body %}
    <h1>Siparis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sipari.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ sipari.title }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ sipari.keywords }}</td>
            </tr>
            <tr>
                <th>Descriptions</th>
                <td>{{ sipari.descriptions }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ sipari.adress }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ sipari.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sipari.image }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ sipari.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ sipari.createdAt ? sipari.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ sipari.updatedAt ? sipari.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ sipari.detail }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ sipari.userid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_siparis_index') }}">back to list</a>

    <a href="{{ path('siparis_edit', {'id': sipari.id}) }}">edit</a>

    {{ include('siparis/_delete_form.html.twig') }}
{% endblock %}
